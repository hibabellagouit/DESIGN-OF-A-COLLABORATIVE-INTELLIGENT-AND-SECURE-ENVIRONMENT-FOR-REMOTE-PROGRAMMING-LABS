services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - DATABASE_URL=mongodb://db:27017/salles
    depends_on:
      - db
  db:
    image: mongo:7
    volumes:
      - mongo_data:/data/db
volumes:
  mongo_data: